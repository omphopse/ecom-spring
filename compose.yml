services:
  dbservices:
    image: mysql
    container_name: ecomportal
    ports:
      - "3308:3306"
    environment:
      MYSQL_ROOT_PASSWORD: Om@123
      MYSQL_DATABASE: ecom
    volumes:
      - D:/docker_data/EcomPRoject:/var/lib/mysql